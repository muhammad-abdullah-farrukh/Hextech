# Project-wide venv (create at the repo root — see requirements.txt / docs/README.md)
.venv/
venv/

# Python bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Local environment / secrets (the .env lives in ocr-resume-paser/)
.env
