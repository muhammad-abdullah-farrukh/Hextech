[
  {
    "name": "candidate_name",
    "type": "string",
    "required": true,
    "description": "Full legal name"
  },
  {
    "name": "email",
    "type": "string",
    "required": true,
    "description": "Primary contact email"
  },
  {
    "name": "phone",
    "type": "string",
    "required": false,
    "description": "Primary contact phone number"
  },
  {
    "name": "linkedin",
    "type": "string",
    "required": false,
    "description": "LinkedIn profile URL or handle, if present (e.g. 'linkedin.com/in/jane')"
  },
  {
    "name": "github",
    "type": "string",
    "required": false,
    "description": "GitHub profile URL or handle, if present (e.g. 'github.com/jane')"
  },
  {
    "name": "location",
    "type": "string",
    "required": false,
    "description": "The candidate's own location / mailing address as written (city, country, or full address)"
  },
  {
    "name": "date_of_birth",
    "type": "string",
    "required": false,
    "description": "Date of birth, only if explicitly stated in the resume"
  },
  {
    "name": "years_experience",
    "type": "object",
    "required": true,
    "description": "Total professional experience as years + months, computed from the work history date ranges",
    "properties": [
      { "name": "years", "type": "integer", "required": true, "description": "Whole years of professional experience" },
      { "name": "months", "type": "integer", "required": true, "description": "Additional months beyond the whole years (0-11)" }
    ]
  },
  {
    "name": "skills",
    "type": "array",
    "items": "string",
    "required": true,
    "description": "Technical skills list"
  },
  {
    "name": "work_history",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Employment entries",
    "properties": [
      { "name": "company", "type": "string", "required": true },
      { "name": "title", "type": "string", "required": true },
      { "name": "location", "type": "string", "required": false, "description": "Where the role was based (city, country), if stated" },
      { "name": "start_date", "type": "string", "required": false },
      { "name": "end_date", "type": "string", "required": false }
    ]
  },
  {
    "name": "projects",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Projects the candidate built or contributed to",
    "properties": [
      { "name": "name", "type": "string", "required": true, "description": "Project name or short title (keep any qualifier such as '(Final Year Project)')" },
      { "name": "description", "type": "string", "required": false, "description": "What the project does / the candidate's contribution" },
      { "name": "technologies", "type": "array", "items": "string", "required": false, "description": "Tools, languages, or frameworks used" },
      { "name": "metrics", "type": "array", "items": "string", "required": false, "description": "Quantitative results reported for the project, each as a short string (e.g. '78.58% accuracy', 'F1 0.91', '110,414 samples', '5,000+ annotations')" },
      { "name": "start_date", "type": "string", "required": false, "description": "When the project started, as written, if stated" },
      { "name": "end_date", "type": "string", "required": false, "description": "When the project ended, as written (or 'present'), if stated" },
      { "name": "location", "type": "string", "required": false, "description": "Project location (city, country), if stated" }
    ]
  },
  {
    "name": "education",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Education entries",
    "properties": [
      { "name": "institution", "type": "string", "required": true },
      { "name": "degree", "type": "string", "required": false },
      { "name": "location", "type": "string", "required": false, "description": "Where the institution is (city, country), if stated" },
      { "name": "gpa", "type": "string", "required": false, "description": "GPA/CGPA or marks as written (e.g. 'CGPA 2.62/4.0', '70/100%', '82.73%'), if stated" },
      { "name": "start_date", "type": "string", "required": false, "description": "When the program started (as written, e.g. '09/2023', 'Sept 2021')" },
      { "name": "end_date", "type": "string", "required": false, "description": "When the program ended, as written (e.g. '2025', 'present', 'expected 2027'); use 'present' for ongoing programs" },
      { "name": "graduation_year", "type": "string", "required": false, "description": "Completion/graduation year ONLY. Leave null if the program is ongoing ('present'/'current') or the year is not stated — never copy the start year here" }
    ]
  },
  {
    "name": "languages",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Spoken/written human languages the candidate knows (not programming languages)",
    "properties": [
      { "name": "name", "type": "string", "required": true, "description": "Language name, e.g. 'English', 'Urdu'" },
      { "name": "proficiency", "type": "string", "required": false, "description": "Stated proficiency level, e.g. 'C2', 'Native', 'Fluent', 'A1'" }
    ]
  },
  {
    "name": "certifications",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Professional certifications, licenses, or credentials the candidate has earned",
    "properties": [
      { "name": "name", "type": "string", "required": true, "description": "Certification/credential name" },
      { "name": "issuer", "type": "string", "required": false, "description": "Issuing organization, if stated" },
      { "name": "year", "type": "string", "required": false, "description": "Year obtained, if stated" }
    ]
  },
  {
    "name": "activities",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "Memberships, extracurriculars, volunteering, or notable non-work activities (e.g. society memberships, campus initiatives)",
    "properties": [
      { "name": "name", "type": "string", "required": true, "description": "Activity, role, or membership name" },
      { "name": "organization", "type": "string", "required": false, "description": "Associated organization or society, if any" },
      { "name": "date", "type": "string", "required": false, "description": "Date or period of the activity, as written, if stated" },
      { "name": "description", "type": "string", "required": false, "description": "What the activity involved, if described" }
    ]
  },
  {
    "name": "references",
    "type": "array",
    "items": "object",
    "required": false,
    "description": "People listed as references for the candidate",
    "properties": [
      { "name": "name", "type": "string", "required": true, "description": "Reference's full name" },
      { "name": "title", "type": "string", "required": false, "description": "Their title/role and affiliation, if stated" },
      { "name": "contact", "type": "string", "required": false, "description": "Their email, phone, or profile link, if stated" },
      { "name": "relationship", "type": "string", "required": false, "description": "How the reference knows the candidate / their stated relationship, if described" }
    ]
  }
]
